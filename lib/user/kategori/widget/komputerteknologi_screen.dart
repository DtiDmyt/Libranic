import 'package:flutter/material.dart';

class KomputerteknologiScreen extends StatelessWidget {
  const KomputerteknologiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Komputer Teknologi')),
      body: const Center(child: Text('Content for Komputer Teknologi')),
    );
  }
}
